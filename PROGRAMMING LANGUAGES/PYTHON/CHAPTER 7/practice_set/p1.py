num=int(input("enter the number::"))
for i in range(1,11):
    # print(str(num)+'x'+str(i)+'='+str(i*num))
    
    print(f"{num}x{i}={num*i}")  
        #   both are the formula ..we can use upper formula as well by using f string
        # we can also find the table of any number